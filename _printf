.TH man 3 "09 Nov 2021" "_printf man page"
. SH NAME
printf - prints a string with specifier(s) and modifier(s)
.SH SYNOPSIS
#include "main.h"
int _printf(const char *format, ...)

.SH DESCRIPTION

The function _printf() produce output according to a format as described below.  The function _printf() write output to stdout. 
In C programming language, _printf() function used to print the character, string, and integer values onto the output screen.

_printf() function with %d format specifier displays the value of an integer variable. Similarly %c is used to display character, %s for string variable.

.RS 3
c   prints a character variable
.br
s   prints a string variable
.br
d   prints an integer and takes an integer value as a signed decimal integer
.br
i   prints an integer and takes in a decimal, type
.br
.RE

.SH RETURN
The function will return the number of characters printed. Null or -1 when string empty among other unsuccesfull cases.

.SH EXAMPLES
To print "Who needs the library? I have Google!" you would do as follows:

 #include "main.h"
 _printf(h%s", "Who needs the library? I have Google!");

.SH BUGS
No known bugs.

.SH AUTHOR
### :email: mail :email:

* [Carlos Andres Pardo](3904@holbertonschool.com) 
* [Juan Sebastian Avendaño](3822@holbertonschool.com)